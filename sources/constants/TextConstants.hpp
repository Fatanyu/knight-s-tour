#pragma once

#include <string>


/**
 * @brief Static class which will have often used strings
 */
struct TextConstants
{
private:
    TextConstants() = default;

public:
    inline static const std::string dashLine = "-----------------------------------";

};